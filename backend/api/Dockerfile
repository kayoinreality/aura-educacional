FROM node:20-alpine AS production

WORKDIR /app

# Placeholder mínimo para permitir build da imagem enquanto o app é ajustado.
COPY . .

EXPOSE 3001

CMD ["sh", "-c", "echo 'API container iniciado. Ajuste build/start do app para produção.' && sleep infinity"]
