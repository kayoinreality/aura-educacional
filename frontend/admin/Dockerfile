FROM node:20-alpine AS production

WORKDIR /app

# Placeholder mínimo para permitir build da imagem enquanto o app é ajustado.
COPY . .

EXPOSE 3002

CMD ["sh", "-c", "echo 'ADMIN container iniciado. Ajuste build/start do app para produção.' && sleep infinity"]
